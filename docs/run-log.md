# Run log

Append-only. One line per run, exploratory or otherwise.

Required by rule 1 / ADR-0006. Exploratory runs do not need a PREREG, but they do need to be
visible: a single unregistered run is not the threat, an unrecorded *number* of them is.
Without this the multiplicity behind any later registered result is unrecoverable.

Log the run even when it fails, and especially when the result is uninteresting — those are
the ones that go unrecorded and inflate everything else.

| Date | Model(s) | Stimuli | What was run | Outcome (one line) |
|---|---|---|---|---|
| 2026-08-05 | — | ecog_v0 (39), mrs_v0 (18), recist_v0 (15) | `--dry-run`, `--check-tokens`, offline readout-safety checks | No model. ECOG/mRS separable bare-only; RECIST separable both conventions. No vocabulary leaks in ECOG. |
| 2026-08-05 | gemma-3-4b-it, medgemma-4b-it | Round-1 eligibility graphs | `reproduce_round1.py` (re-verification, offline) | Reproduced: cross-model jaccard 0.13–0.19, `age_pos` missing (0-byte graph). |
| 2026-08-06 | medgemma-4b-it, gemma-3-4b-it | ecog_v0 (39), mrs_v0 (18) | `run_ecog_stimuli.py`, RTX 5090 pod — **crashed** | NameError in `build_results` (`GRADE_QUESTION`, stale after 6ec0484). Scored and printed, wrote no JSON. Same deterministic computation as the row below, not an independent look. |
| 2026-08-06 | medgemma-4b-it, gemma-3-4b-it | ecog_v0 (39) | `run_ecog_stimuli.py`, RTX 5090 pod, both models same session | **Gemma grading falls monotonically with lexical distance: 100% verbatim → 82% paraphrase → 64% inferred → 50% distractor. MedGemma flat at ~60%.** Eligibility 22/39 and 25/39. Self-consistency 24/39 and 29/39 — the pillar-2 null is high. Readout clean, `probe_canonical_mass` ≥ 0.999, both surfaces calibrated `bare`. Grades not yet clinician-adjudicated. |
| 2026-08-06 | medgemma-4b-it, gemma-3-4b-it | mrs_v0 (18) | `run_ecog_stimuli.py`, same session | Smoke test of the per-intermediate config on real weights — it works. Eligibility 13/18 and 15/18. **Not a measurement**: 18 rows, no paraphrase sets, 9 rows name the scale. |
| 2026-08-21 | gemma-3-4b-it, medgemma-4b-it | ecog_v0 (39), mrs_v0 (18) | `rescore_results.py` then `pillar2_baselines.py`, offline re-analysis of the 2026-08-06 outputs — no new inference | **Scorer bug: `pred_eligible_from_grade` applied the intermediate default to every row, so both reversed-threshold distractors were scored against the criterion they were written to violate.** Fixed; self-consistency ECOG 24→23 and 29→28, mRS 11→12 and 16→17. Against a constant-answer baseline no ECOG pillar-2 number clears p<0.05: Gemma eligibility 22/39 *is* the always-No baseline exactly, coupling p=0.21; MedGemma coupling p=0.12. Opposite response biases (Gemma 31/39 No, MedGemma 23/39 Yes). |
| 2026-08-22 | gemma-3-4b-it, medgemma-4b-it, gemma-3-12b-it, gemma-3-27b-it, medgemma-27b-text-it | ecog_sweep_v0 (234 rows = 39 vignettes x 6 criteria) | `run_ecog_stimuli.py` then `flip_point.py`, H100 SXM pod, all five models same session | **Capability threshold: the 27B applies the criterion, the 4B does not.** Gemma 4B coupling 1/36 is *below* the always-Yes baseline and it says No at `ECOG <= 4` on 24/36; Gemma 27B coupling 11/36 p=0.0013 is the only cell clearing baseline, and survives Bonferroni over the 10 tests run. Pattern-matching collapses with scale — echo rate on the reversed `ECOG >= 2` criterion 0.81 → 0.69 → 0.39 at 4B/12B/27B, the most robust finding here. MedGemma's 4B advantage vanishes at 27B. Gates passed: readout clean on all five, **grade determinism 0/39 drift on all five**, monotonicity 0.86 → 1.00. Full write-up: `docs/program/pod-run-2026-08-22-results.md`. |
| 2026-08-22 | — | ecog_sweep_v0 results (offline re-analysis, no new inference) | `flip_point.py` baseline fix + `--selftest` | **Scorer bug: one baseline used for two different tests.** `base` was the true-grade-0 count, correct for `flip==true` but wrong for `flip==own`, where always-Yes hits wherever the model's *own* grade is 0 — so grading leniency was being credited as application. Fixed to two matched baselines. MedGemma 4B coupling p 0.133 → **0.816** (below its own baseline); Gemma 27B p 0.029 → **0.0013**. New `grade-0-collapse` selftest archetype (grade everything 0, answer Yes to everything) scores 39/39 coupling while computing nothing — read as p=1.98e-32 under the old baseline. Tests 107 → 112. **Known remaining gap: the direction check still has no baseline** (always-Yes scores 21/36 free; no model clears it). |
| 2026-08-22 | gemma-3-27b-it, gemma-3-4b-it | ecog_sweep_v0, 4 donor/receiver pairs | `patch_grade.py` (Track C, residual-stream patching at the final position), same H100 session | `--selftest` passed on pod torch 2.8.0+cu128 (identity 0.00e+00, dominance 0.00e+00). **The answer flips in both models at roughly mid-depth — the 4B flips too.** 27B first flip 27-54 of 62 layers (44-87% depth), 4B 16-19 of 34 (47-56%); the 4B's one non-flip is a degenerate pair where its always-No bias makes donor and receiver agree. So the 4B/27B difference is not the presence of load-bearing final-position content but what it encodes. **Confounded two ways**: last-layer dominance makes deep flips near-tautological, and patching the full residual stream transfers the whole prompt rather than an isolated grade. **Same-grade control not yet run** — `patch_grade.py` refuses same-grade pairs by design; needs a flag before this supports any claim. |
| 2026-08-22 | all five (4b pair, 12b, 27b pair) | **mrs_sweep_v0 (108 rows = 18 vignettes x 6 criteria)** — new, generated by `make_sweep_stimuli.py --intermediate mrs` | `run_ecog_stimuli.py` then `flip_point.py`, same H100 session | **Generalisation probe; it does not generalise, and the reason is the stimulus set.** Headline mRS numbers look far stronger than ECOG (27B coupling 6/18 p=0.011, MedGemma 27B 8/18 p=0.005, direction check clears for *all five* models incl. the 4B). But **50% of mrs_sweep rows name the defining vocabulary** (0% for ecog_sweep), and splitting on `leaks_vocab` shows the coupling lives entirely in the leaking half: on the 9 vignettes that state the score, 12B/27B/MedGemma-27B clear (4/9, 5/9, 5/9); on the 9 that require recovery, **none of the five clear** (1/9, 3/9, 1/9, 1/9, 3/9). The mRS coupling result is a lookup effect. Confirms CLAUDE.md's existing 'not a measurement' warning on mrs_v0. n=9 per half is far below the 22-52 sizing rule, so this is 'no evidence', not 'evidence of no effect'. **mRS cannot serve as the generalisation test until its vignettes are rewritten without the defining vocabulary and expanded past n=22.** ECOG results unaffected (0% leak). |
| 2026-08-23 | gemma-3-12b-it, gemma-3-27b-it | one short prompt | `ct_feas.py` — attribution feasibility probe, H100 80GB | **The 4B-only constraint on attribution graphs is false.** Gemma Scope 2 covers the whole Gemma 3 family and circuit-tracer supports it; circuit-tracer-format sets are `mwhanna/gemma-scope-2-{12b,27b}-it`, referenced as a plain path with an explicit variant (`.../transcoder_all/width_16k_l0_small_affine`) — a bare repo id fails on `config.yaml`. Measured: 12B loads 48 transcoders, 29.6GB, peak **36.8GB**; 27B loads 62, 64.7GB, peak **76.5GB** of 79.2GB usable. Both `ATTRIBUTE_OK`. Also: the cu121 pin in `setup_pod_circuit_tracer.sh` is unnecessary (works on torch 2.8.0+cu128), and `lazy_decoder=True` is why the static estimate of 74GB was too high. **27B has no margin** — at default `max_n_logits=10` on a real 60-token prompt it OOMs at 77.5GB. |
| 2026-08-23 | gemma-3-27b-it | ecog_sweep_v0, 6 vignettes x 2 adjacent criteria | `ct_contrastive.py` (Track B at 27B) then `compare_graphs.py` | **12/12 graphs, completeness 0.706-0.755, every pair inverts as designed.** Required `offload=cpu`, `batch_size=48`, `max_n_logits=5`, `max_feature_nodes=1500`; defaults OOMed on all 12. **Result: changing one digit of the criterion perturbs the circuit more than replacing the entire patient vignette** — jaccard 0.613 (criterion differs) vs 0.782 (patient differs), complete separation, Mann-Whitney U=36/36, exact one-sided p=0.0011. Feature ids verified stable across graphs (592/592, 656/656) and graph sizes comparable, so this is not the size-confounded jaccard that broke a Round-1 claim. **Caveat: 93% of influence sits on answer scaffolding** (final positions plus the literal ' Yes'/' No' tokens), criterion span and patient text ~0.1% each — pruning at `node_threshold=0.8` and a memory-forced `max_feature_nodes=1500` may be discarding the criterion-position features. Lower-threshold rerun on an H200 before any claim rests on these. Completeness at 27B is below the 4B Stage 1 graphs (0.80-0.85). Nodes unlabelled. |
| 2026-08-23 | gemma-3-27b-it | the 12 contrastive graphs (offline re-analysis, no new inference) | feature-recurrence and confound check on the Track B graphs | **WITHDRAWS the previous row's headline.** The jaccard contrast was confounded: within-vignette pairs differ in the criterion *and* flip the answer, while the control held the answer fixed. Separating them, the effect is entirely the answer flip — same-answer 0.741 vs different-answer 0.566 across 30+30 cross-vignette pairs, U=878, z=6.33, p=1.2e-10; holding the flip constant there is no criterion effect (criterion pairs are *more* similar, as sharing the vignette text predicts). What replaces it: **the graphs are dominated by answer readout.** Every answer-selective feature (clean 6/0 across six vignettes and three thresholds) sits on the literal ' Yes'/' No' token; **8 of 11,170 nodes (0.07%) touch the criterion span**; 93% of influence is on answer scaffolding. Two criterion features recur and are worth following: `L0 f32130` on the token '1' in all four `<= 1` graphs (trivial digit detector) and **`L56 f116346` on '4' in the <=4 graph and '3' in the <=3 graph** — a late-layer feature tracking the threshold value whatever it is, the only real candidate here, n=2. Lower-threshold rerun on an H200 is now a prerequisite. |
| 2026-08-23 | gemma-3-27b-it | ecog_sweep_v0, same 6 contrastive pairs | `graph_contrastive.py` rerun on **H200 141GB**, `max_feature_nodes=4000`, `node_threshold=0.95`, `max_n_logits=10`, no offload | **The previous row's "almost no criterion machinery" was a pruning artefact.** H200 removes the memory ceiling entirely (64.6GB after load, no OOM, no offload needed). Criterion-span coverage **0.072% -> 1.619% of nodes** (8 -> 700) and **~0.1% -> 1.75% of influence**, while total nodes grew only 3.9x — the loosening added criterion nodes disproportionately, which is what a selection artefact predicts. Distinct criterion-span features ~4 -> **137**; **65 recur across >=4 of 6 vignettes**. Named candidates: `L9 f2258865`/`L36 f90457938`/`L13 f308491` on the ECOG scale tokens (6/6 vignettes), **`L8 f1293627` on `' <='` (6/6)**, and `L56 f116346` on the threshold digit across 3 distinct values. Positional evidence only — nodes unlabelled, causal weight needs ablation (rule 3). Completeness 0.753-0.760, slightly above the tight run. **Lesson: report node-selection settings with any claim about graph content; the tight run supported a confident negative that was false.** The jaccard withdrawal replicates here (answer effect p=7.0e-11). |
