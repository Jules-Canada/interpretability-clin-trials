# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.venv/
venv/
dist/
build/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# macOS
.DS_Store

# Data — large cached activations, never commit
data/

# Model checkpoints — large, regenerable
checkpoints/

# Attribution-graph viewer: an upstream clone, not our code. Provenance (URL +
# pinned commit) is in docs/ops.md. Holds frontend/graph_data/ — 1.1GB of results,
# two files over GitHub's 100MB hard limit. Regenerable via run_graphs_ct.py.
frontend/

# Weights & Biases
wandb/

# Editor / Claude
.claude/
.pytest_cache/

# Secrets
.env
